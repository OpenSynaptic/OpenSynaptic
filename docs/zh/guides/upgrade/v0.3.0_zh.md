---
layout: default
title: OpenSynaptic v0.3.0 升级指南
language: zh
---

# OpenSynaptic v0.3.0 升级指南

本指南涵盖从 `v0.2.0` 到 `v0.3.0` 的迁移。

- 预计时间：15-30 分钟
- 兼容性：现有核心工作流向后兼容
- 受众：运维人员、后端工程师和发布维护人员

---

## 快速开始（5 分钟）

### 选项 A：升级现有部署（推荐）

```powershell
cd .
git pull origin main
python -u verify_deployment.py
python -u src/main.py plugin-test --suite stress --total 5000 --workers 8
```

### 选项 B：全新设置

```powershell
cd .
pip install -e .
python -u verify_deployment.py
python -u test_id_lease_system.py
```

---

## 完整程序

### 1) 准备

```powershell
python -u src/main.py --version
Copy-Item Config.json Config.json.backup
```

### 2) 更新代码

```powershell
git pull origin main
```

如果你无法使用 Git，请手动复制更新的文件，包括 `docs/`、`scripts/`、`README.md`、`CHANGELOG.md` 和 `AGENTS.md`。

### 3) 验证部署

```powershell
python -u verify_deployment.py
```

预期结果：部署检查通过，环境准备就绪。

### 4) 验证 ID 租赁行为（推荐）

```powershell
python -u test_id_lease_system.py
```

### 5) 审计驱动程序能力

```powershell
python -u scripts/audit_driver_capabilities.py
```

预期结果：所有目标驱动程序都报告完整的发送 + 接收/监听支持。

### 6) 运行性能完整性检查

```powershell
python -u src/main.py plugin-test --suite stress --total 10000 --workers 8 --processes 2 --threads-per-process 4 --batch-size 64
```

将吞吐量和尾延迟与之前的基线进行对比。

---

## 配置说明

在 `Config.json` 中使用 `security_settings.id_lease` 进行租赁策略调优。

参考文档：

- `docs/ID_LEASE_SYSTEM.md`
- `docs/ID_LEASE_CONFIG_REFERENCE.md`

示例基线：

```json
{
  "security_settings": {
    "id_lease": {
      "offline_hold_days": 30,
      "base_lease_seconds": 2592000,
      "min_lease_seconds": 0,
      "rate_window_seconds": 3600,
      "high_rate_threshold_per_hour": 60,
      "ultra_rate_threshold_per_hour": 180,
      "ultra_rate_sustain_seconds": 600,
      "high_rate_min_factor": 0.2,
      "adaptive_enabled": true,
      "ultra_force_release": true,
      "metrics_emit_interval_seconds": 5
    }
  }
}
```

---

## 升级检查清单

- [ ] 代码已更新到 v0.3.0
- [ ] `verify_deployment.py` 已通过
- [ ] ID 租赁测试已通过
- [ ] 驱动程序能力审计已通过
- [ ] 压力完整性运行已完成，无回归
- [ ] `Config.json` 备份已保留

---

## 故障排除

### 缺少文档文件

如果缺少文档文件，请重新同步存储库文件并验证 `docs/` 树。

### 升级后出现导入错误

```powershell
pip install -e .
```

### 损坏的 ID 分配文件

```powershell
Remove-Item data/id_allocation.json
python -u test_id_lease_system.py
```

### 版本不匹配

```powershell
python -u -c "from opensynaptic import __version__; print(__version__)"
```

---

## 后续步骤

1. 阅读 `docs/ID_LEASE_SYSTEM.md` 了解生命周期细节。
2. 运行 `scripts/integration_test.py` 以获得集成信心。
3. 查看 `docs/guides/drivers/quick-reference.md` 了解监听/发送模式。
4. 在发布门中跟踪压力指标（`p95`、`p99`、`p99.9`）。
